# Expense Manager Backend (FastAPI + MongoDB)

This is the backend for an expense management app using FastAPI and MongoDB.

## Setup
1. Install dependencies:  
## To Run
use command: uvicorn app.main:app --reload